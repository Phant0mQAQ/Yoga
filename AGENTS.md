# Repository workflow

- 每次完成代码、配置或文档更新后，除非用户明确要求不要推送，否则必须在相关检查通过后提交并直接推送到 GitHub 的 `main`。
- 仓库只保留 `main` 分支；不要创建或保留功能分支。若因外部工具临时产生分支，合并后立即删除。
- 不得绕过失败的测试、构建检查或仓库保护规则。
- 不得提交 `.env`、真实密钥、令牌、证书、构建产物、依赖目录或 Wrangler 本地状态；仅提交明确标记为示例的环境变量模板。
- Cloudflare 部署应对应已推送到 `main` 的 Git 提交，并在交付说明中记录提交哈希和部署版本。
- 每次推送代码、配置或文档更新到 GitHub `main` 后，必须同步部署到 Cloudflare，并验证线上版本与该提交一致。
- 不要编造文献；所有引用的文献都需要有明确出处。
