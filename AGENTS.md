# Repository workflow

- 每次完成代码、配置或文档更新后，除非用户明确要求不要推送，否则应在相关检查通过后提交并推送到 GitHub。
- 默认通过分支和 Pull Request 合并到 `main`，不要绕过失败的测试或保护规则。
- 不得提交 `.env`、真实密钥、令牌、证书、构建产物、依赖目录或 Wrangler 本地状态；仅提交明确标记为示例的环境变量模板。
- Cloudflare 部署应对应已推送的 Git 提交，并在交付说明中记录提交哈希和部署版本。
- 不要编造文献；所有引用的文献都需要有明确出处。
